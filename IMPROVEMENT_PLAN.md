# SPALAM 改善計画

日付: 2026-07-07

本ドキュメントは、現状実装の分析に基づき、既知の3つの課題に対する改善策を提案するものです。

1. オブジェクトのトラッキング精度が 8th Wall にわずかに劣る
2. オブジェクトがカメラの画角外に一度出るとトラッキングを継続できない(現状は方向のみで再配置しており、画角外にいる間のカメラ移動距離が失われる)
3. パフォーマンス

## 現状アーキテクチャの要約 (as-is)

現在の毎フレームパイプラインは以下の通りです。

| ステージ | 実装 | 場所 |
|---|---|---|
| 特徴点トラッキング | Sobel勾配強度画像上での Shi-Tomasi + Lucas-Kanade オプティカルフロー(200コーナー、50%解像度、中央60% ROI) | `src/FeatureDetector.ts:212-246` |
| 姿勢(向き) | DeviceOrientation のクォータニオンと視覚推定を固定アルファの相補フィルタで融合(IMU 0.9 / 視覚 0.1) | `src/tracking/ComplementaryFilter.ts` |
| 並進(奥行き軸のみ) | `DistanceTracker` — 特徴点ペア間距離の比率からスケール係数を推定(接近/後退のみ) | `src/tracking/DistanceTracker.ts`(結線箇所: `src/SPALAM.ts:1528-1548`) |
| ドリフト補正 | 安定した視覚特徴点を参照した IMU 姿勢の周期補正(1000ms間隔) | `src/tracking/DriftCorrector.ts` |
| 深度推定 | Depth-Anything-V2-small(504×504、WebGPU fp32)。アンカーの3D位置初期化に使用 | `src/DepthEstimation.ts` |
| 画角外からの復帰 | フラスタム判定 → 中央領域で特徴点再検出 → 中心特徴点を深度で逆投影してオブジェクトを再配置 | `src/SPALAM.ts:1304-1400`, `1072-1112` |

**構造上の重要な発見:** `src/tracking/` には完全な 6DoF SLAM バックエンドが既に存在します — `PoseEstimator`(エッセンシャル行列)、`PnPSolver`、`Triangulator`、`LocalBundleAdjustment`、`IMUPreintegration`、`ScaleEstimator`、`VisualInertialOptimizer`、`MotionModel`、`DescriptorMatcher` — しかし、**これらはいずれもライブループに結線されていません**(検証済み: `SPALAM.ts` から参照されているのは `DistanceTracker` のみ)。したがって、ランタイムが推定しているのは実質「3DoF の向き + 1DoF のスケール」であり、メトリックな 6DoF カメラ姿勢ではありません。以下の3課題はすべてこのギャップに帰着するため、最もレバレッジが高い作業は新しいアルゴリズムの開発ではなく、既存モジュールの統合です。

ただし、「バックエンドを結線する」だけでは不十分です。単眼SLAMは、初期化の退化、純回転、低テクスチャ、ローリングシャッター、動的物体で日常的に破綻します。以下の横断的な前提条件を先に設計しなければ、結線したパイプラインは脆弱なものになります。

---

## 横断的な前提条件(Phase 1 の統合作業より先に設計すべき事項)

1. **トラッキング状態機械。** 明示的な状態 — `initializing`、`tracking`、`visual-degraded(IMU補助)`、`lost/relocalizing`、`frozen` — を定義し、状態遷移と状態ごとの描画挙動を決める。課題2のあらゆる復帰機能はこの上に構築されるため、後付けは非常に困難。
2. **タイムスタンプの整合。** カメラフレーム、`DeviceMotion`/`DeviceOrientation` イベント、描画時刻はそれぞれ異なるクロック・異なるレイテンシ上にある。単一の時間基準(例: `performance.now()` ドメイン)を確立し、カメラ–IMU 間のレイテンシオフセットを明示的に推定する。これなしでは、姿勢の平滑化を超える IMU-視覚融合は性能が出ない。
3. **Web IMU の現実的な限界の検証。** `DeviceOrientationEvent` は OS が融合済みの姿勢であり、生のジャイロ値ではない。`DeviceMotionEvent` は角速度と加速度を約60Hzで提供するが、ハードウェアタイムスタンプもバイアス推定もない。これはネイティブの ARKit/ARCore(および WebAssembly 側でフィルタリングする 8th Wall)が前提とする IMU アクセスよりはるかに弱い。密結合VIOにコミットする前に、どの程度の preintegration 品質が達成可能かを実測で検証すること。IMU は主に回転の事前情報および短時間のブリッジとして扱う。
4. **外れ値とマップポイントのライフサイクル管理。** RANSAC ベースの PnP、トラックごとの経過フレーム数と再投影誤差履歴、ランドマークの観測回数、視差しきい値、マップポイントの間引き(culling)。不良ランドマークがマップに残存すると、BA による精度向上はすぐに帳消しになる。
5. **セッションの堅牢性。** visibility change / バックグラウンド化、カメラ解像度の再ネゴシエーション、画面回転、権限の取り消しに対して、マップを静かに破壊するのではなく、定義された状態へ遷移して復旧する。

---

## 課題1: トラッキング精度が 8th Wall にわずかに劣る

### 根本原因

1. **メトリックな 6DoF カメラ姿勢が存在しない。** 向き(IMU支配)+ 単一スケール係数では、カメラの横方向・上下方向の並進を表現できない。横移動はすべてオブジェクトのドリフトとして現れる(レンダリングカメラが並進しないため)。
2. **アンカーの3D位置が単発のニューラル深度に由来する。** 単眼深度推定はノイズが多く、フレーム間でスケールが一貫しないため、アンカーはそのノイズを恒久的に引き継ぐ(`FeatureAnchor` は生成時に `position3D` を固定する)。
3. **固定 0.9/0.1 の相補フィルタ重み**は視覚側の信頼度を無視している — 視覚が良好なときは活用不足、視覚が不良なときも 10% がノイズを注入し続ける。
4. **特徴点品質の制約:** 50%解像度・中央60% ROI 内の200コーナーでは、特徴点が少なく、精度が低く、分布も偏る。
5. **仮定ベースの内部パラメータ:** `focalLengthPixels` が指定されない限り、焦点距離は仮定の水平FOVから導出される(`src/helpers/backProjectPoints.ts:21-44`)。8th Wall はデバイスごとのキャリブレーションを持つ。誤った焦点距離はすべての逆投影と姿勢推定にバイアスを与える。

### 改善策(優先度順)

#### 1-A. 既存の 6DoF パイプラインの結線(最重要)

既存モジュールを使い、標準的な単眼VIOのフロントエンド/バックエンド分割を採用する:

- **ブートストラップ:** 2つのキーフレーム間で `PoseEstimator`(エッセンシャル行列)を使う。ただし「最初の2フレーム」ではなく実質的な条件でゲートする: 十分な視差、十分なインライア数、純回転でない運動、ホモグラフィよりエッセンシャルモデルが明確に優位であること(平面のみ・回転のみのシーンでは初期化を遅延させる)。`Triangulator` で初期ランドマークを三角測量し、`ScaleEstimator`(IMU)と深度ネットワークを事前情報としてスケールを固定する — **メトリックスケールの整合ポリシーはここで決める**。後回しにするとマップのスケールとアンカー深度が乖離する。
- **毎フレームのトラッキング:** 向き+スケールに頼るのではなく、三角測量済みマップポイントに対する **3D–2D PnP**(`PnPSolver`)を解く。前提条件セクションの外れ値ライフサイクル規則に基づく RANSAC でラップする。PnP はメトリックな並進を与え、2D–2D の連鎖のようにフレーム間ドリフトが蓄積しない。
- **バックエンド:** キーフレームに対して `LocalBundleAdjustment`(スライディングウィンドウ実装済み、ウィンドウ7)を実行し、姿勢とランドマークを refinement する。フレームループをブロックしないようスロットリング/非同期化する。BA を有効化する前に、キーフレーム選択とマップポイント culling を整備しておくこと。

#### 1-B. アンカーの幾何は三角測量で決める(ニューラル深度は事前情報に格下げ)

アンカーの深度は深度ネットワークで初期化し、視差のある2枚以上のキーフレームから観測された時点で **多視点三角測量により `position3D` を refinement** する。深度ネットワークは事前情報/スケールヒントとしてのみ保持する。これがアンカー位置誤差の最大要因を除去する。

補正は**制限付き・低頻度の更新**として適用する(1秒あたりのステップ上限を設け、ユーザーがオブジェクトを操作している間は行わない)。ユーザー視点では絶対精度より視覚的安定性のほうが重要であり、「より正しい位置」へ目に見えてジャンプするアンカーは、トラッキングの劣化として認識される。

#### 1-C. 適応的なセンサー融合

固定アルファの `ComplementaryFilter` を信頼度重み付き融合に置き換える: 視覚側の重みは PnP インライア率**および**再投影誤差の統計量で決める(インライア率単独では信頼度指標として不十分)。視覚の信頼度が下がるにつれて IMU 単独側へフォールバックする。これには前提条件セクションのタイムスタンプ整合が先に必要。長期的には `IMUPreintegration` のファクターを `VisualInertialOptimizer` に投入して密結合融合を行う — ただし Web IMU の実測検証でセンサー品質が十分と確認できた場合のみ。積分の入力には OS 融合済みの `DeviceOrientation` クォータニオンではなく、生ジャイロに近い `DeviceMotionEvent.rotationRate` を使うこと。

#### 1-D. 特徴点の品質

- **グリッドバケッティング:** フレームをセル(例: 8×6)に分割し、セルごとの特徴点数クォータを設けて画像全体をカバーさせる — 姿勢推定精度は特徴点の空間分布に強く依存する。
- **Forward–backward LK チェック:** prev→curr と curr→prev の両方向で追跡し、往復誤差が約0.5pxを超える点を棄却する。低コストで不良トラックの大半を除去できる。
- 新規検出コーナーへの **`cornerSubPix` によるサブピクセル refinement**。グローバル検出は50%解像度のままでも、アンカー特徴点は小パッチ単位でフル解像度追跡することを検討する。
- `MotionModel`(等速モデル、実装済み)でオプティカルフローの探索初期位置をシードし、高速移動時の収束を改善する。

#### 1-E. 内部パラメータのキャリブレーション

- 利用可能な場合は `MediaTrackSettings`/`getCapabilities()` の焦点距離ヒントを読む — ただし大半のブラウザでは取得できない前提で、機会的な補助として扱う。解像度変更、デジタルズーム、手ぶれ補正、クロップにより**実効的な**内部パラメータはセッション中にも変化する点に注意。
- 現実的な短期対策として、デバイスモデルごとの小さな FOV テーブルを保守する(商用 WebAR エンジンが実質的にやっていることと同じ)。
- BA 内での焦点距離の自己校正は最終フェーズ限定とする: 通常の手持ち運動では、焦点距離・主点・歪み係数が強く相関し、推定値がマップ全体を歪める可能性がある。強い視差ゲートと上下限を設けた場合のみ有効化する。

#### 1-F. ローリングシャッターへの意識

モバイルカメラはローリングシャッターであり、高速回転時にはすべての計測(LK、エッセンシャル行列、PnP)が同時に歪む。完全な RS 補償はスコープ外だが、安価な緩和策は可能: ジャイロで高角速度を検出したウィンドウでは、RANSAC しきい値を引き上げ、キーフレーム/ランドマークの生成を抑制し、IMU の回転事前情報に寄せる。

---

## 課題2: オブジェクトが画角外に出た際のトラッキング永続化

### 根本原因

現システムは**ワールド座標系におけるカメラ**ではなく、**オブジェクトの特徴点**を追跡している。オブジェクトが画角外に出ると追跡対象が消滅し、再進入時には `repositionToCenterFeature()` が現在のカメラを基準に(方向+深度で)オブジェクトを再配置するため、画角外にいる間のカメラ並進は破棄される。

### 改善策: モデルの反転 — ワールド固定のオブジェクト + 追跡されるカメラ

これは 8th Wall のモデルそのものである: オブジェクトのワールド姿勢は一度設定したら再計算せず、更新するのはカメラ姿勢のみ。具体的には:

#### 2-A. 環境全体の特徴マップ(前提。1-A と共通)

- 中央60% ROI 制限を撤廃し、グリッドバケッティング(1-D)を用いてフレーム全体で特徴点を検出・追跡する。
- 対象平面上だけでなく、あらゆる場所でランドマークを三角測量する。オブジェクトが画角外にある間も、カメラは見えているランドマークに対する PnP で自己位置推定を継続するため、**パンして視線を外している間も並進はメトリックに保たれ**、フラスタムが再びオブジェクトを含んだ時点で単に再描画すればよい — 正常系では再配置ステップ自体が不要になる。

#### 2-B. IMU デッドレコニングによる短時間ギャップのブリッジ

視覚トラッキングが劣化する瞬間(モーションブラー、無テクスチャの壁)への対策:

- 回転: ジャイロ積分(`IMUPreintegration` で利用可能)— こちらは信頼できる部分であり、ブリッジの主役とすべき。
- 並進: Web API の加速度の二重積分は1秒未満でドリフトするため、期待値は控えめに — ごく短いウィンドウ(数百ms)のみで使用し、補正量をクランプし、並進ブリッジの主手段としては等速 `MotionModel` による外挿を優先する。ZUPT(ゼロ速度更新)は静止判定が確実な場合にのみ有効で、誤った「静止」判定はむしろ姿勢を破壊するため、保守的にゲートする。短いウィンドウを超えたら並進を凍結し、relocalization(2-C)に委ねる。

#### 2-C. 長時間ギャップに対するキーフレーム relocalization

- オブジェクト配置時およびその後周期的に、オブジェクト近傍の長寿命な**アンカーキーフレーム**(姿勢+特徴点+記述子)を保存する。現状の7フレームBAウィンドウは古いキーフレームを破棄するため、その外側に保持する。
- トラッキング喪失時または不確かさが大きいとき、現在フレームの記述子を(SIFT実装済みの `DescriptorMatcher` で)キーフレームDBと照合し、マッチに対して PnP を解いてカメラ姿勢を補正する。目に見えるスナップを避けるため、補正は数フレームかけてブレンドする。
- relocalization には固有の堅牢性レイヤーが必要: 候補マッチの幾何検証(RANSAC PnP インライアしきい値)、複数キーフレームがマッチした場合の姿勢仮説スコアリング、明示的な誤検出棄却 — オブジェクトをテレポートさせる誤った relocalization は、lost のままでいるより悪い。照合コストが問題になる場合は、DB 用記述子として SIFT より軽量なバイナリ記述子(ORB/BRIEF)を優先する。
- 現在の方向ベースの `repositionToCenterFeature()` は、relocalization が失敗した場合の最終フォールバックとしてのみ残す。

#### 2-D. メトリックスケールの一貫性

永続化はスケールがギャップをまたいで安定している場合にのみ成立する。`ScaleEstimator`(IMU 加速度 vs 視覚並進)を継続的に実行し、深度ネットワークの事前情報と整合させることで、ギャップ前後の姿勢が同一のメトリック座標系に収まるようにする。

---

## 課題3: パフォーマンス

### 推奨する作業順序

#### 3-A. まず計測する — ただし後回しにせず Phase 1 で行う

各ステージ(キャプチャ、Sobel、LK、深度、姿勢、描画)の前後に `performance.mark()`/`measure()` を仕込み、デモにステージ別 ms のオーバーレイを表示する。これと基本的なフレームタイムバジェットのガードは、Phase 1 のトラッキング作業と**同時に**投入しなければならない: PnP + 三角測量 + キーフレーム管理の追加は毎フレームコストを増やし、フレーム落ちはトラッキング品質を直接劣化させる(オプティカルフローのベースラインが伸び、LK の失敗が増える)。ここではパフォーマンスは独立した作業項目ではなく、トラッキング精度の機能である。

#### 3-B. CV パイプラインをメインスレッドから降ろす

現状、Sobel + オプティカルフロー + トラッキングロジックが Three.js と同じメインスレッドで動いている。CV パイプライン全体を1つのワーカーへ移す:

- フレームは `createImageBitmap(video)`(transferable)または WebCodecs の `VideoFrame` で供給し、ワーカー内の `OffscreenCanvas` 上で OpenCV 処理を行う。ブラウザ差に注意: `VideoFrame` の transferability とコピーコストは環境により異なり、WebCodecs は普遍的ではない — `createImageBitmap` パスをベースラインとして実装し、`VideoFrame` は最適化として扱う。
- Three.js の描画はメインスレッドに残す(`OffscreenCanvas` によるワーカー側 Three.js はブラウザ間で挙動が不安定)。ワーカーは姿勢の更新値を返送する。
- これによりメインスレッドの毎フレーム `drawImage` + `getImageData` 読み戻しも除去される。

#### 3-C. カメラフレームに同期する

`requestAnimationFrame` + 手動の33msゲーティング(`src/SPALAM.ts:1124-1130`)を **`requestVideoFrameCallback`** に置き換える。実際のカメラフレームごとに1回発火するため、変化のないフレームの重複処理や、ディスプレイとカメラのレート差によるうなり(ビート)由来のカクつきがなくなる。rAF パスはフォールバックとして残す(古い Safari/WebView のサポートが不完全)。副次的な利点として、rVFC のメタデータはキャプチャタイムスタンプを提供し、前提条件の IMU タイムスタンプ整合に利用できる。

#### 3-D. 深度ネットワークのコスト削減

マップベースのトラッキング(1-A/1-B)が入れば、ニューラル深度が必要なのはアンカー初期化とスケール事前情報のみになる:

- キーフレーム時(約10フレームごと)またはオンデマンドでのみ実行する。
- 量子化モデル(transformers.js の `dtype` オプションで fp16/q8)や、より小さい入力(例: 384×384 または 252×252)へ切り替える — 事前情報としての精度で十分。
- トラッキングループを深度の出力待ちで絶対にブロックしない: WebGPU には初回のシェーダーコンパイルによるストール、`device lost` イベント、モバイルでのサーマルスロットリングがある。深度の結果は非同期に届く事前情報として扱う。

#### 3-E. OpenCV.js の効率化

- 素の CDN ビルドではなく **SIMD ビルド**の OpenCV.js を配信する。LK と Sobel は SIMD で大幅に速くなる。スレッドはさらに cross-origin isolation(COOP/COEP ヘッダー)を要求するが、WebAR ライブラリの埋め込み先ではヘッダーを設定できないことが多い — SIMD をベースラインの改善、スレッドはオプションとして扱う。
- LK ピラミッドを再利用する: `buildOpticalFlowPyramid` でフレームごとに1回だけピラミッドを構築し、`calcOpticalFlowPyrLK` にピラミッドを渡すことで、各画像のピラミッドが(prev として・curr として)2回計算されるのを防ぐ。
- 代替案として、ソースは既に canvas/テクスチャなので、Sobel を WebGL/WebGPU のフラグメントシェーダーへ移すこともできる。

#### 3-F. アロケーションと適応品質の衛生管理

- `FeatureDetector` で実施済みの Mat プーリングを `PoseEstimator`/PnP 系のパスにも拡張する(`findEssentialMat`/`solvePnP` ラッパー内の毎フレーム `cv.Mat` 生成)。
- 動的な品質スケーリング: 計測したフレームタイムがバジェットを超えたら `maxCorners` と処理解像度を段階的に下げ、余裕が戻ったら復元する。

---

## ロードマップ案

| Phase | 内容 | 対応課題 |
|---|---|---|
| 1 | 基盤 + 6DoF フロントエンド: ステージ別プロファイリングとフレームバジェット、トラッキング状態機械、タイムスタンプ整合、全画面グリッド特徴点 + forward–backward チェック、条件ゲート付きエッセンシャル行列初期化、三角測量 + マップポイントライフサイクル、ライブループでの RANSAC PnP、メトリックスケールポリシー | 課題1、2(根本原因)、3(ガードレール) |
| 2 | バックエンド品質: キーフレーム選択、非同期ローカルBA、制限付きアンカー三角測量 refinement、信頼度重み付き融合、深度事前情報の統合 | 課題1 |
| 3 | 永続化: 長寿命アンカーキーフレーム + 検証付き relocalization、環境ランドマークに対する画角外カメラトラッキング、短時間の IMU/運動モデルブリッジ | 課題2 |
| 4 | パフォーマンス深掘り: ワーカー側 CV パイプライン、`requestVideoFrameCallback`(+ フォールバック)、キーフレーム限定の量子化深度、SIMD OpenCV、ピラミッド再利用、動的品質スケーリング | 課題3 |
| 5 | 8th Wall パリティ(実証ゲート付き): `VisualInertialOptimizer` による密結合VIOと焦点距離自己校正 — Web IMU の実測検証(前提条件 §3)でセンサー品質が十分と示された場合にのみ採用 | 課題1 |

Phase 1–3 は主として `src/tracking/` に既に存在するテスト付きコードの統合であり、計画のリスクを大幅に下げる。外部レビューから得た最大の教訓: リスクはアルゴリズム(実装済み)ではなく、**状態管理、外れ値ライフサイクル、初期化のゲート条件、Web のタイミング/センサー制約**にある — そのためこれらを Phase 1 に前倒ししている。
